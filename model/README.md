# Computer Vision Model (`/model`)

This directory contains all code related to the computer vision model:
- `dataset.py`: Dataset loading, data augmentation, and data split handling.
- `train.py`: Model architecture initialization and training pipeline.
- `predict.py`: Prediction interface that accepts a single image and returns a class label prediction.
